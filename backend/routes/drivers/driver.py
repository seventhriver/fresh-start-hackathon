class Driver:
    def __init__(self, name, email, assigned_orders, phone, session_token):
        self.name = name
        self.email = email
        self.assigned_orders = assigned_orders
        self.phone = phone
        self.session_token = session_token
